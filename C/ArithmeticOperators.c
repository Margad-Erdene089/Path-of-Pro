#include<stdio.h>
int main()
{
    int a = 10, b = 5;
    int sum , minus , multi, division, Modulo;
    // sum -- niilber 
    // minus -- hasah
    // Multi(Multiplication)-- vrjih
    // Division -- huvaah
    // Modulo -- vldegdel avah
    sum = a + b;
    minus = a - b;
    multi = a * b;
    division = a / b;
    Modulo = a%b;
    printf ("%d\n%d\n%d\n%d\n%d", sum , minus , multi , division , Modulo);
    return 0 ;
}